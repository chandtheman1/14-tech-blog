<h1>The Tech Blog</h1>

{{#each blogs as |blog|}}
<section class="card">
    <a href="/blog/{{blog.id}}">
        <h2>{{blog.title}}</h2>
        <p>{{blog.content}}</p>
    </a> 
</section>
{{/each}}
